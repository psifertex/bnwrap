# TODO

- [ ] Modify the color schemes of each tab to be different so the collage looks better
- [ ] Change the layout of the collage to put tiles in a grid
- [ ] Bind ESC to cancel key so it's easier to close the dialog
- [x] Fix exporting all images as a collage
- [x] Make quotes per-section instead of global at the bottom and add more variety
- [x] Make UI plugin so you don't have to open a BV first
- [x] Fix static/dynamic detection using imports
- [x] Add a 3s delay to ensure splash screen always shows